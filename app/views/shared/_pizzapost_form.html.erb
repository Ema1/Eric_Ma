<%= form_for(@pizzapost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    Pizz Form </br>
    <%= f.text_area :content, placeholder: "Compose new pizzapost..." %>
  </div>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>